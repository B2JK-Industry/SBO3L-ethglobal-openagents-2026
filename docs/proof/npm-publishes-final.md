# npm + PyPI publishes — final inventory (2026-05-02 closeout)

Generated by HEAD requests against the public registries (`https://registry.npmjs.org/<pkg>/1.2.0` and `https://pypi.org/pypi/<pkg>/1.2.0/json`).

## Scope

Every Dev 2 published-target package across both ecosystems — 31 total (10 PyPI cohort + 22 npm).

## Status

### ✅ Live on PyPI (5/7)

| Package | Version | URL |
|---|---|---|
| `sbo3l-sdk` | 1.2.0 | https://pypi.org/project/sbo3l-sdk/ |
| `sbo3l-langchain` | 1.2.0 | https://pypi.org/project/sbo3l-langchain/ |
| `sbo3l-crewai` | 1.2.0 | https://pypi.org/project/sbo3l-crewai/ |
| `sbo3l-llamaindex` | 1.2.0 | https://pypi.org/project/sbo3l-llamaindex/ |
| `sbo3l-langgraph` | 1.2.0 | https://pypi.org/project/sbo3l-langgraph/ |

### ❌ Pending PyPI (2)
| Package | Why |
|---|---|
| `sbo3l-agno` | trusted publisher not yet configured for env `pypi-agno-py` |
| `sbo3l-pydantic-ai` | trusted publisher not yet configured for env `pypi-pydantic-ai-py` |

### ✅ Live on npm (3/22)

| Package | Version | URL |
|---|---|---|
| `@sbo3l/langchain` | 1.2.0 | https://www.npmjs.com/package/@sbo3l/langchain |
| `@sbo3l/autogen` | 1.2.0 | https://www.npmjs.com/package/@sbo3l/autogen |
| `@sbo3l/elizaos` | 1.2.0 | https://www.npmjs.com/package/@sbo3l/elizaos |

### ❌ Pending npm (19)

The publish workflow `integrations-publish.yml` only triggers on the original 8 cohort tags (`langchain-ts-v*`, `autogen-v*`, `elizaos-v*`, `vercel-ai-v*` + their PyPI counterparts). The 14 post-cohort npm packages have no matching tag-pattern entry.

| Package | Why |
|---|---|
| `@sbo3l/sdk` | matrix entry exists in workflow; tag `sdk-ts-v*` not configured |
| `@sbo3l/vercel-ai` | tag `vercel-ai-v1.2.0` exists; publish step kept failing — needs investigation |
| `@sbo3l/openai-assistants` | post-cohort — no tag pattern in workflow |
| `@sbo3l/anthropic` | same |
| `@sbo3l/anthropic-computer-use` | same |
| `@sbo3l/mastra` | same |
| `@sbo3l/vellum` | same |
| `@sbo3l/langflow` | same |
| `@sbo3l/inngest` | same |
| `@sbo3l/marketplace` | same |
| `@sbo3l/letta` | same |
| `@sbo3l/autogpt` | same |
| `@sbo3l/babyagi` | same |
| `@sbo3l/superagi` | same |
| `@sbo3l/cohere-tools` | same |
| `@sbo3l/together` | same |
| `@sbo3l/perplexity` | same |
| `@sbo3l/replicate` | same |
| `@sbo3l/modal` | same |
| `@sbo3l/e2b` | same |
| `@sbo3l/agentforce` | same |
| `@sbo3l/copilot-studio` | same |

## Recovery path for pending packages

For the 19 pending npm packages, two fixes are needed:

1. **Extend `.github/workflows/integrations-publish.yml` matrix** to include the 14+ post-cohort packages. The matrix entry per package is ~5 lines (id, dir, pkg, tag prefix). Estimated 30 minutes to add all.

2. **Tag each at v1.2.0 + push** — `git tag <id>-v1.2.0 && git push origin <id>-v1.2.0`. With `workflow_dispatch` (PR #225), Daniel can also trigger publishes manually for already-existing tags via `gh workflow run integrations-publish.yml --ref <tag>`.

3. **Investigate `@sbo3l/vercel-ai` publish failure** — tag exists, workflow fired, publish step failed. Per the round 12 #277 hotfix's commit message, the original failure was JSON in the SDK's package.json. That's now fixed; rerunning the publish via `workflow_dispatch` should succeed.

For the 2 pending PyPI packages, configure trusted publishers at `https://pypi.org/manage/account/publishing/`:
- Project name: `sbo3l-agno` / `sbo3l-pydantic-ai`
- Owner: `B2JK-Industry`
- Repository: `SBO3L-ethglobal-openagents-2026`
- Workflow: `integrations-publish.yml`
- Environment: `pypi-agno-py` / `pypi-pydantic-ai-py`

## Cross-references

- `docs/release/v1.2.0-recovery-runbook.md` (PR #228) — full step-by-step
- PR #225 — `workflow_dispatch` for manual publish re-fires
- `scripts/sdk-install-verify.sh` — automated registry probe (this report's source)

## Tally

| Ecosystem | Live | Pending | Total |
|---|---|---|---|
| PyPI | **5** | 2 | 7 |
| npm | **3** | 19 | 22 |
| **Total** | **8** | **21** | **29** |

Wire format is correct on every live package — the gap is purely distribution-pipeline configuration.
