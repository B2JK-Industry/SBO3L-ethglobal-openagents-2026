# Quickstart static-validation matrix

Generated by `scripts/quickstart-validate.sh` on 2026-05-02T11:08:57Z.

## Per-guide status

| Guide | File | Status |
|---|---|---|
| `keeperhub-with-langchain` | `docs/quickstart/keeperhub-with-langchain.md` | ✅ ok |
| `keeperhub-with-openai-assistants` | `docs/quickstart/keeperhub-with-openai-assistants.md` | ✅ ok |
| `uniswap-with-vercel-ai` | `docs/quickstart/uniswap-with-vercel-ai.md` | ✅ ok |
| `uniswap-with-mastra` | `docs/quickstart/uniswap-with-mastra.md` | ✅ ok |
| `ens-with-anthropic` | `docs/quickstart/ens-with-anthropic.md` | ✅ ok |

## Issues

None — all guides pass static validation.

## What this script checks

1. Each `npm i` / `pip install` block mentions all expected packages
2. Every expected SDK export name appears in the guide's code
3. APRP fixture has all 12 v1-required fields
4. No frozen `01HTAWX5...` nonces (round-4 codex P1: tripped `protocol.nonce_replay`)
5. No expired `2026-05-0?T10:31:00Z` expiries
6. Guide calls `crypto.randomUUID` or `uuid.uuid4` (proves dynamic nonces)
7. `expected_recipient` matches the reference policy's allowlist (`0x1111...1111` on chain `base`)

## What this script does NOT check

- Runtime end-to-end (LLM-driven). Needs the npm packages live AND model API keys.
- The `sdk-install-matrix` workflow (`scripts/sdk-install-verify.sh`, PR #239) covers package liveness;
  Heidi runs each quickstart manually for the LLM-driven verification.

## Re-run locally

```bash
./scripts/quickstart-validate.sh
ONLY=ens-with-anthropic ./scripts/quickstart-validate.sh
```
