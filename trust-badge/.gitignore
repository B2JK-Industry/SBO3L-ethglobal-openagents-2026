# Generated by trust-badge/build.py — never hand-edit, never commit.
# build.py reads demo-scripts/artifacts/latest-demo-summary.json and writes
# this file. The directory itself stays tracked via this .gitignore.
index.html
