# Generated by demo-scripts/run-openagents-final.sh — never hand-edit, never commit.
# The directory itself is tracked via this .gitignore so the demo runner can
# write to it without `mkdir`-style errors.
*.json
